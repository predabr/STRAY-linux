import { ExternalLink } from "lucide-react";
import { useEffect, useState } from "react";

export function WorkspaceStatus({ label, version }: { label: string; version: string }) {
  const [online, setOnline] = useState(() => navigator.onLine);
  useEffect(() => { const connected = () => setOnline(true); const disconnected = () => setOnline(false); window.addEventListener("online", connected); window.addEventListener("offline", disconnected); return () => { window.removeEventListener("online", connected); window.removeEventListener("offline", disconnected); }; }, []);
  return <div className="product-rail-status mt-4 px-2 pt-4"><p className="inline-flex items-center gap-2 font-tech text-[10px] uppercase tracking-[.12em] text-emerald-300"><span className="product-rail-status-dot h-1.5 w-1.5 rounded-full bg-emerald-300" />{label}</p>{!online ? <p className="mt-2 font-tech text-[9px] uppercase tracking-[.12em] text-amber-200">Offline · dados locais ativos</p> : null}<p className="mt-2 font-tech text-[10px] text-white/35">{version}</p><a href="/changelog" className="mt-3 inline-flex items-center gap-1.5 text-xs text-white/45 transition-colors hover:text-cyan-100">Notas da versão<ExternalLink className="h-3 w-3" /></a><a href="https://linuxtoys-ckuyvpj5.manus.space/support" target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-1.5 text-xs text-white/45 transition-colors hover:text-cyan-100">Apoio ao projeto<ExternalLink className="h-3 w-3" /></a><a href="https://linuxtoys-ckuyvpj5.manus.space/uninstall" target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-1.5 text-xs text-white/45 transition-colors hover:text-cyan-100">Guia de desinstalação<ExternalLink className="h-3 w-3" /></a></div>;
}
