import { PageBreadcrumbs } from "@/components/platform/PageBreadcrumbs";
import { SiteHeader } from "@/components/platform/SiteHeader";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Switch } from "@/components/ui/switch";
import { localeMeta, SUPPORTED_LOCALES, useLanguage } from "@/contexts/LanguageContext";
import { useTheme } from "@/contexts/ThemeContext";
import { Bell, CloudOff, Eye, Globe2, Monitor, RefreshCw, ShieldCheck, SlidersHorizontal } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

type LocalSettings = { performanceMonitoring: boolean; gameDetection: boolean; launchConfirmation: boolean; updateChecks: boolean };
const key = "stray-desktop-settings-v1";
const defaults: LocalSettings = { performanceMonitoring: false, gameDetection: true, launchConfirmation: true, updateChecks: false };
function readSettings(): LocalSettings { try { return { ...defaults, ...JSON.parse(localStorage.getItem(key) || "{}") }; } catch { return defaults; } }

export default function SettingsCenter() {
  const { theme, setTheme } = useTheme(); const { locale, setLocale } = useLanguage(); const [settings, setSettings] = useState<LocalSettings>(readSettings); const [notice, setNotice] = useState<string | null>(null);
  const update = (field: keyof LocalSettings, value: boolean) => { const next = { ...settings, [field]: value }; setSettings(next); localStorage.setItem(key, JSON.stringify(next)); setNotice("Preferência salva neste dispositivo."); };
  const reset = () => { if (!window.confirm("Redefinir preferências locais? Isso não remove jogos, perfis, snapshots ou guias salvos.")) return; localStorage.removeItem(key); localStorage.removeItem("stray-sync-preferences-v1"); setSettings(defaults); setTheme("system"); setNotice("Preferências locais redefinidas. Dados pessoais e snapshots foram preservados."); };
  return <div className="min-h-screen bg-background"><SiteHeader /><main className="container max-w-[1440px] technical-grid py-5 md:py-7"><div className="space-y-6"><PageBreadcrumbs items={[{ label: "Pessoal", href: "/dashboard" }, { label: "Configurações" }]} /><header className="stray-surface rounded-2xl p-5"><p className="stray-kicker">SETTINGS / LOCAL-FIRST</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Controle do aplicativo e da privacidade.</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">As preferências desta tela ficam no dispositivo. O Stray não ativa telemetria, compartilhamento de performance ou ações de IA destrutivas por padrão.</p></header>{notice ? <p className="rounded-xl border border-emerald-400/20 bg-emerald-400/[0.04] p-3 text-sm text-emerald-200">{notice}</p> : null}<section className="grid gap-5 xl:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Monitor className="h-5 w-5 text-primary" />Aparência e idioma</CardTitle><CardDescription>Preferências visuais locais e reversíveis.</CardDescription></CardHeader><CardContent className="space-y-5"><div><p className="text-sm font-medium">Tema</p><div className="mt-2 flex flex-wrap gap-2">{(["dark", "light", "system"] as const).map((option) => <Button key={option} variant={theme === option ? "default" : "outline"} size="sm" onClick={() => setTheme(option)}>{option === "dark" ? "Escuro" : option === "light" ? "Claro" : "Sistema"}</Button>)}</div></div><label className="block text-sm font-medium">Idioma<select className="mt-2 h-10 w-full rounded-md border bg-background px-3 text-sm" value={locale} onChange={(event) => setLocale(event.target.value as typeof locale)}>{SUPPORTED_LOCALES.map((item) => <option key={item} value={item}>{localeMeta[item].nativeName}</option>)}</select></label></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><SlidersHorizontal className="h-5 w-5 text-primary" />Detecção e uso local</CardTitle><CardDescription>Controles que não escrevem arquivos de jogos nem executam comandos.</CardDescription></CardHeader><CardContent className="space-y-3"><SwitchRow label="Descoberta de jogos" detail="Permite iniciar leituras locais de Steam e Heroic quando você solicitar." checked={settings.gameDetection} onChange={(value) => update("gameDetection", value)} /><SwitchRow label="Métricas de performance" detail="Desativado por padrão. O Stray não coleta FPS sem um coletor local autorizado." checked={settings.performanceMonitoring} onChange={(value) => update("performanceMonitoring", value)} /><SwitchRow label="Confirmar lançamento Steam" detail="Mantém a confirmação antes de abrir um URI steam:// de jogo detectado." checked={settings.launchConfirmation} onChange={(value) => update("launchConfirmation", value)} /><SwitchRow label="Verificar atualizações" detail="Desativado por padrão. Nenhum executável é baixado automaticamente." checked={settings.updateChecks} onChange={(value) => update("updateChecks", value)} /></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-primary" />Centro de privacidade</CardTitle><CardDescription>O estado abaixo descreve o comportamento atual, não uma promessa de sincronização.</CardDescription></CardHeader><CardContent className="space-y-3"><PrivacyRow label="Dados locais" value="Ativos" detail="Scanner, biblioteca, snapshots e sessões ficam neste dispositivo." icon={Eye} /><PrivacyRow label="Telemetria" value="Desativada" detail="O aplicativo não envia telemetria obrigatória." icon={CloudOff} /><PrivacyRow label="Performance compartilhada" value="Desativada" detail="Métricas só podem existir quando importadas ou submetidas de forma explícita." icon={Globe2} /><PrivacyRow label="Dados para IA" value="Contexto local" detail="O Stray AI desktop usa somente perfil e conteúdo local; não executa ações." icon={ShieldCheck} /><Link href="/sync"><Button className="mt-2 w-full" variant="outline">Revisar consentimento de sincronização</Button></Link></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Bell className="h-5 w-5 text-primary" />Atualizações e redefinição</CardTitle><CardDescription>Atualizações exigem uma fonte de release conhecida e confirmação do usuário.</CardDescription></CardHeader><CardContent className="space-y-3 text-sm leading-6 text-muted-foreground"><p>Não há download automático, instalador em segundo plano ou alteração silenciosa de versão. Use o site de download para consultar release notes e SHA-256.</p><a className="inline-flex text-primary hover:underline" href="/download">Abrir downloads e checksums</a><Button className="w-full" variant="outline" onClick={reset}><RefreshCw className="mr-2 h-4 w-4" />Redefinir preferências locais</Button></CardContent></Card></section></div></main></div>;
}

function SwitchRow({ label, detail, checked, onChange }: { label: string; detail: string; checked: boolean; onChange: (value: boolean) => void }) { return <div className="flex items-center justify-between gap-4 rounded-xl border p-4"><div><p className="text-sm font-medium">{label}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{detail}</p></div><Switch checked={checked} onCheckedChange={onChange} aria-label={label} /></div>; }
function PrivacyRow({ label, value, detail, icon: Icon }: { label: string; value: string; detail: string; icon: typeof Eye }) { return <div className="flex gap-3 rounded-xl border bg-card/40 p-3"><Icon className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><div><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-medium">{label}</p><span className="font-tech text-[9px] uppercase tracking-[.1em] text-emerald-300">{value}</span></div><p className="mt-1 text-xs leading-5 text-muted-foreground">{detail}</p></div></div>; }
