import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowDown, ArrowRight, Check, Clipboard, ClipboardCheck, Download, ExternalLink, ShieldCheck, Sparkles, Terminal } from "lucide-react";
import { Button } from "@/components/ui/button";
import { distributionAssets as assets, linuxInstallers } from "@/lib/distribution";

type Installer = (typeof linuxInstallers)[number];
type StoryPanel = { kicker: string; title: React.ReactNode; text: string; note: string; icon: typeof ShieldCheck };

const storyPanels: StoryPanel[] = [
  { kicker: "01 / LEIA O AMBIENTE", title: <>Seu Linux.<br /><em>Sem suposições.</em></>, text: "Stray Linux organiza leituras locais, guias e diagnósticos em uma camada clara para quem joga no Linux.", note: "Nada é enviado por padrão.", icon: ShieldCheck },
  { kicker: "02 / ENTENDA O CONTEXTO", title: <>Evidência antes<br /><em>da resposta.</em></>, text: "O aplicativo distingue sinais observados, fontes publicadas e lacunas de informação. Sem FPS, nota ou compatibilidade inventados.", note: "Fatos, inferências e desconhecidos ficam separados.", icon: Sparkles },
  { kicker: "03 / MANTENHA O CONTROLE", title: <>Atualize quando<br /><em>você decidir.</em></>, text: "Instalações, importações e atualizações exigem uma ação explícita. O Stray não executa comandos do sistema por conta própria.", note: "Feed HTTPS e confirmação antes de reiniciar.", icon: Terminal },
];

export default function Home() {
  const [selected, setSelected] = useState<Installer["id"] | null>(null);
  const [copied, setCopied] = useState(false);
  const installer = useMemo(() => selected ? linuxInstallers.find((item) => item.id === selected) : undefined, [selected]);
  const copyTerminal = async () => { if (!installer) return; await navigator.clipboard?.writeText(installer.command); setCopied(true); window.setTimeout(() => setCopied(false), 1800); };
  return <div id="top" className="min-h-screen overflow-x-hidden bg-[#050505] text-[#f5f5f2] selection:bg-white selection:text-black"><SiteNav /><main><HorizontalStory /><MobileStory /><section id="downloads" className="border-y border-white/10 bg-[#f5f5f2] py-16 text-[#0a0a0a] sm:py-24"><div className="container"><div className="max-w-2xl"><p className="font-tech text-[10px] font-bold tracking-[.18em] text-black/55">DOWNLOADS VERIFICÁVEIS / V1.1.0</p><h2 className="mt-4 text-4xl font-semibold tracking-[-.07em] sm:text-6xl">Agora escolha<br />o formato certo.</h2><p className="mt-5 text-sm leading-7 text-black/62 sm:text-base">Cada opção baixa um artefato publicado. Em Linux, o comando só aparece depois da escolha da família e valida o SHA-256 antes da instalação.</p></div><div className="mt-10 grid gap-4 lg:grid-cols-[.7fr_1.3fr]"><WindowsDownload /><TerminalInstaller selected={selected} setSelected={setSelected} installer={installer} copied={copied} copyTerminal={copyTerminal} /></div></div></section></main><SiteFooter /></div>;
}

export function DownloadPage() {
  const [selected, setSelected] = useState<Installer["id"] | null>(null);
  const [copied, setCopied] = useState(false);
  const installer = useMemo(() => selected ? linuxInstallers.find((item) => item.id === selected) : undefined, [selected]);
  const copyTerminal = async () => { if (!installer) return; await navigator.clipboard?.writeText(installer.command); setCopied(true); window.setTimeout(() => setCopied(false), 1800); };
  return <div className="min-h-screen bg-[#f5f5f2] text-[#0a0a0a]"><SiteNav inverse /><main className="container py-16 sm:py-24"><p className="font-tech text-[10px] font-bold tracking-[.18em] text-black/55">STRAY LINUX / DOWNLOADS</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.075em] sm:text-7xl">Instale com<br /><em className="font-normal">o formato certo.</em></h1><p className="mt-6 max-w-2xl text-base leading-8 text-black/62">Windows usa instalador direto. Em Linux, escolha a família para obter somente o pacote, o checksum e o comando compatíveis.</p><div className="mt-10 grid gap-4 lg:grid-cols-[.7fr_1.3fr]"><WindowsDownload /><TerminalInstaller selected={selected} setSelected={setSelected} installer={installer} copied={copied} copyTerminal={copyTerminal} /></div></main><SiteFooter inverse /></div>;
}

function HorizontalStory() {
  const sectionRef = useRef<HTMLElement>(null); const [progress, setProgress] = useState(0);
  useEffect(() => { let frame = 0; const update = () => { cancelAnimationFrame(frame); frame = requestAnimationFrame(() => { const section = sectionRef.current; if (!section || window.innerWidth < 900 || window.matchMedia("(prefers-reduced-motion: reduce)").matches) { setProgress(0); return; } const range = Math.max(1, section.offsetHeight - window.innerHeight); setProgress(Math.min(1, Math.max(0, -section.getBoundingClientRect().top / range))); }); }; update(); window.addEventListener("scroll", update, { passive: true }); window.addEventListener("resize", update); return () => { cancelAnimationFrame(frame); window.removeEventListener("scroll", update); window.removeEventListener("resize", update); }; }, []);
  return <section ref={sectionRef} className="relative hidden h-[330vh] border-b border-white/10 lg:block"><div className="sticky top-0 h-screen overflow-hidden"><div className="absolute inset-0 grid-mask opacity-30" aria-hidden="true" /><div className="absolute left-0 top-0 h-px bg-white/70 transition-[width] duration-75" style={{ width: `${progress * 100}%` }} /><div className="absolute left-8 top-24 z-10 font-tech text-[10px] tracking-[.2em] text-white/45">STRAY LINUX / SCROLL TO EXPLORE</div><div className="absolute bottom-8 left-8 z-10 flex items-center gap-2 font-tech text-[10px] tracking-[.16em] text-white/45"><ArrowDown className="h-3.5 w-3.5" />ROLE PARA AVANÇAR</div><div className="flex h-full w-[300vw] will-change-transform" style={{ transform: `translate3d(-${progress * 66.6667}%,0,0)` }}>{storyPanels.map((panel, index) => <StoryPanelView key={panel.kicker} panel={panel} index={index} progress={progress} />)}</div></div></section>;
}

function StoryPanelView({ panel, index, progress }: { panel: StoryPanel; index: number; progress: number }) { const Icon = panel.icon; const local = Math.min(1, Math.max(0, progress * 3 - index)); return <article className="relative flex h-full w-screen shrink-0 items-center px-8 py-24 xl:px-[max(8vw,8rem)]"><div aria-hidden="true" className="absolute inset-0 opacity-100" style={{ background: index === 1 ? "radial-gradient(circle at 74% 42%, rgba(255,255,255,.12), transparent 25%)" : index === 2 ? "radial-gradient(circle at 62% 68%, rgba(255,255,255,.08), transparent 30%)" : "radial-gradient(circle at 82% 18%, rgba(255,255,255,.1), transparent 22%)" }} /><div className="relative grid w-full max-w-[1200px] grid-cols-[1.15fr_.85fr] items-end gap-16"><div style={{ opacity: .2 + local * .8, transform: `translate3d(${(1 - local) * 56}px,0,0)` }} className="transition-[opacity,transform] duration-150"><p className="font-tech text-[11px] font-bold tracking-[.2em] text-white/55">{panel.kicker}</p><h1 className="mt-7 text-[clamp(4.5rem,9vw,10rem)] font-semibold leading-[.78] tracking-[-.1em] text-white">{panel.title}</h1><p className="mt-10 max-w-xl text-lg leading-8 text-white/64">{panel.text}</p></div><aside className="border-l border-white/20 pl-8 pb-2" style={{ opacity: local, transform: `translate3d(0,${(1 - local) * 36}px,0)` }}><Icon className="h-6 w-6 text-white" /><p className="mt-8 font-tech text-[10px] tracking-[.16em] text-white/42">SINAL DE PRODUTO</p><p className="mt-3 max-w-xs text-xl leading-7 text-white">{panel.note}</p><div className="mt-12 flex items-center gap-2 font-tech text-[10px] tracking-[.15em] text-white/45">{index < 2 ? <>PRÓXIMO PAINEL <ArrowRight className="h-3.5 w-3.5" /></> : <>DOWNLOADS A SEGUIR <ArrowDown className="h-3.5 w-3.5" /></>}</div></aside></div></article>; }

function MobileStory() { return <section className="relative border-b border-white/10 lg:hidden"><div className="absolute inset-0 grid-mask opacity-20" aria-hidden="true" /><div className="container relative space-y-20 py-20">{storyPanels.map((panel) => { const Icon = panel.icon; return <article key={panel.kicker} className="border-l border-white/25 pl-5"><p className="font-tech text-[10px] font-bold tracking-[.18em] text-white/55">{panel.kicker}</p><Icon className="mt-7 h-6 w-6" /><h1 className="mt-7 text-5xl font-semibold leading-[.82] tracking-[-.08em]">{panel.title}</h1><p className="mt-6 text-sm leading-7 text-white/62">{panel.text}</p><p className="mt-6 font-tech text-[10px] tracking-[.13em] text-white/45">{panel.note}</p></article>; })}</div></section>; }

function WindowsDownload() { return <article className="border border-black/20 bg-black p-6 text-[#f5f5f2]"><p className="font-tech text-[10px] tracking-[.15em] text-white/50">WINDOWS 10/11 / X64</p><h3 className="mt-5 text-3xl font-semibold tracking-[-.05em]">Instalador .EXE</h3><p className="mt-4 text-sm leading-6 text-white/62">Baixe o instalador NSIS oficial. Confirme o SHA-256 publicado antes de executar.</p><a href={assets.exe} download="Stray-Linux-1.1.0-Setup.exe" className="mt-8 flex"><Button className="h-12 w-full rounded-none bg-white text-black hover:bg-white/85"><Download className="mr-2 h-4 w-4" />Baixar .EXE</Button></a><p className="mt-4 text-xs leading-5 text-white/42">Atualizações controladas são verificadas no aplicativo instalado.</p></article>; }

function SiteNav({ inverse = false }: { inverse?: boolean }) { const dark = inverse ? "border-black/10 bg-[#f5f5f2]/90 text-black" : "border-white/10 bg-[#050505]/85 text-white"; return <header className={`sticky top-0 z-50 border-b backdrop-blur-xl ${dark}`}><div className="container flex h-16 items-center justify-between"><a href="/" className="flex items-center gap-2.5"><span className={`grid h-8 w-8 place-items-center border ${inverse ? "border-black/20" : "border-white/25"}`}><ShieldCheck className="h-4 w-4" /></span><span className="text-sm font-bold leading-[.85] tracking-tight">STRAY<br />LINUX</span></a><a href="#downloads"><Button size="sm" className={`rounded-none px-3 text-xs ${inverse ? "bg-black text-white hover:bg-black/85" : "bg-white text-black hover:bg-white/85"}`}>Downloads</Button></a></div></header>; }

function SiteFooter({ inverse = false }: { inverse?: boolean }) { return <footer className={inverse ? "border-t border-black/10 bg-[#f5f5f2] text-black/55" : "border-t border-white/10 bg-[#050505] text-white/45"}><div className="container flex flex-col gap-3 py-8 text-xs sm:flex-row sm:items-center sm:justify-between"><span>© 2026 Stray Linux.</span><span>Criado por Pedro, Brasil.</span><a href="/uninstall" className="inline-flex items-center gap-1 hover:text-current">Desinstalação <ExternalLink className="h-3 w-3" /></a></div></footer>; }

function TerminalInstaller({ selected, setSelected, installer, copied, copyTerminal }: { selected: Installer["id"] | null; setSelected: (value: Installer["id"]) => void; installer?: Installer; copied: boolean; copyTerminal: () => Promise<void> }) { return <article className="overflow-hidden border border-black/20 bg-white"><div className="flex items-center justify-between border-b border-black/10 px-5 py-4"><div className="flex items-center gap-2"><Terminal className="h-4 w-4" /><span className="font-tech text-[10px] tracking-[.14em]">INSTALAÇÃO LINUX</span></div><span className="font-tech text-[9px] text-black/45">ESCOLHA A FAMÍLIA</span></div><div className="flex overflow-x-auto border-b border-black/10 px-2">{linuxInstallers.map((item) => <button type="button" key={item.id} onClick={() => setSelected(item.id)} aria-pressed={selected === item.id} className={`shrink-0 border-b-2 px-3 py-3 text-xs transition-colors ${selected === item.id ? "border-black text-black" : "border-transparent text-black/45 hover:text-black"}`}>{item.signal}</button>)}</div>{installer ? <div className="p-5"><div className="border border-black/15 bg-black/[.03] p-3"><p className="font-tech text-[9px] tracking-[.13em] text-black/55">CONFIRMAÇÃO</p><p className="mt-1 text-sm font-medium">{installer.name} · pacote {installer.signal}</p><p className="mt-1 text-xs leading-5 text-black/55">Este bloco baixa, verifica e instala somente o formato escolhido.</p></div><pre className="mt-4 overflow-x-auto bg-black p-4 font-tech text-[11px] leading-6 text-white/75"><code><span className="text-white">$</span> {installer.command}</code></pre><Button onClick={() => void copyTerminal()} variant="outline" className="mt-4 w-full rounded-none border-black/20 bg-white text-black hover:bg-black/5">{copied ? <ClipboardCheck className="mr-2 h-4 w-4" /> : <Clipboard className="mr-2 h-4 w-4" />}{copied ? "Comando copiado" : "Copiar comando"}</Button><p className="mt-4 flex gap-2 text-xs leading-5 text-black/48"><Check className="mt-0.5 h-3.5 w-3.5 shrink-0" />O bloco usa `bash -c`; pode ser colado em fish, zsh ou Bash.</p></div> : <div className="p-5"><div className="border border-black/20 bg-black/[.03] p-4"><p className="font-tech text-[10px] tracking-[.14em] text-black/55">SELEÇÃO OBRIGATÓRIA</p><p className="mt-2 text-sm font-medium">Não copie um comando antes de escolher sua distribuição.</p><p className="mt-2 text-xs leading-5 text-black/58">Use `cat /etc/os-release` para confirmar a família. Arch, CachyOS, EndeavourOS e Garuda usam exclusivamente a aba <strong>pacman</strong>.</p></div></div>}</article>; }
