import { ProvenanceBadge } from "@/components/platform/ProvenanceBadge";
import { SiteHeader } from "@/components/platform/SiteHeader";
import { PageBreadcrumbs } from "@/components/platform/PageBreadcrumbs";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Skeleton } from "@/components/ui/skeleton";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { BookOpenCheck, Check, CheckCircle2, ChevronRight, CircleAlert, Clipboard, Copy, ExternalLink, FileWarning, MessageCircle, Search, Terminal, ThumbsDown, ThumbsUp, Wrench } from "lucide-react";
import { useMemo, useState } from "react";
import { Link, useRoute } from "wouter";

export function WikiPage() {
  const [query, setQuery] = useState("");
  const input = useMemo(() => ({ page: 1, pageSize: 40, q: query.trim() || undefined }), [query]);
  const articles = trpc.knowledge.wiki.list.useQuery(input);
  if (articles.isError) return <PublicFrame eyebrow="LINUX WIKI" title="Consulta indisponível" description="Não foi possível carregar os artigos agora."><ErrorKnowledge returnHref="/wiki" returnLabel="Voltar à wiki" /></PublicFrame>;
  return <PublicFrame eyebrow="LINUX WIKI" title="A referência que começa pela sua distribuição." description="Artigos com fonte, versão e status de publicação. Scores de gaming não são exibidos sem metodologia documentada."><SearchField value={query} onChange={setQuery} placeholder="Pesquisar distros, drivers, Steam e Vulkan" /><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{articles.isLoading ? Array.from({ length: 12 }).map((_, i) => <Skeleton className="h-52 rounded-2xl" key={i} />) : articles.data?.map(({ article, distributionName }) => <Link key={article.id} href={`/wiki/${article.slug}`} className="group rounded-2xl border bg-card p-5 shadow-sm transition-all hover:-translate-y-0.5 hover:border-primary/40 hover:shadow-md"><div className="flex items-center justify-between gap-2"><Badge variant="secondary">{distributionName || article.category}</Badge><ProvenanceBadge provenance={article.provenance} /></div><h2 className="mt-5 text-lg font-semibold tracking-tight">{article.title}</h2><p className="mt-2 line-clamp-3 text-sm leading-6 text-muted-foreground">{article.excerpt}</p><p className="mt-5 flex items-center text-sm font-medium text-primary">Abrir referência <ChevronRight className="ml-1 h-4 w-4 transition-transform group-hover:translate-x-1" /></p></Link>)}</div>{articles.data?.length === 0 ? <EmptyKnowledge icon={BookOpenCheck} title="Nenhum artigo encontrado" text="Tente outro termo ou consulte a lista de distribuições disponíveis." /> : null}</PublicFrame>;
}

export function WikiDetailPage() {
  const [, params] = useRoute("/wiki/:slug");
  const query = trpc.knowledge.wiki.bySlug.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) });
  const item = query.data;
  if (query.isLoading) return <PublicFrame eyebrow="LINUX WIKI" title="Carregando artigo" description=""><Skeleton className="mt-8 h-96 rounded-2xl" /></PublicFrame>;
  if (query.isError) return <PublicFrame eyebrow="LINUX WIKI" title="Consulta indisponível" description="Não foi possível carregar este artigo."><ErrorKnowledge returnHref="/wiki" returnLabel="Voltar à wiki" /></PublicFrame>;
  if (!item) return <PublicFrame eyebrow="LINUX WIKI" title="Artigo não encontrado" description="Este conteúdo pode não estar publicado."><Link href="/wiki"><Button className="mt-6">Voltar à wiki</Button></Link></PublicFrame>;
  return <PublicFrame eyebrow="LINUX WIKI" title={item.article.title} description={item.article.excerpt || "Referência técnica da plataforma."}><article className="mt-8 max-w-4xl rounded-2xl border bg-card p-6 md:p-8"><div className="flex flex-wrap items-center gap-2"><Badge variant="secondary">{item.distributionName || item.article.category}</Badge><ProvenanceBadge provenance={item.article.provenance} />{item.article.versionLabel ? <Badge variant="outline">Versão: {item.article.versionLabel}</Badge> : null}</div><div className="prose prose-sm mt-7 max-w-none whitespace-pre-wrap text-foreground dark:prose-invert">{item.article.body}</div>{item.article.sourceUrl ? <a className="mt-8 inline-flex items-center text-sm font-medium text-primary hover:underline" href={item.article.sourceUrl} target="_blank" rel="noreferrer">Abrir fonte registrada <ExternalLink className="ml-2 h-4 w-4" /></a> : null}</article></PublicFrame>;
}

export function SetupPage() {
  const [query, setQuery] = useState(""); const [difficulty, setDifficulty] = useState<"beginner" | "intermediate" | "advanced" | undefined>(); const [distributionId, setDistributionId] = useState<number>();
  const input = useMemo(() => ({ page: 1, pageSize: 40, q: query.trim() || undefined, difficulty, distributionId }), [difficulty, distributionId, query]); const guides = trpc.knowledge.guides.list.useQuery(input); const distributions = trpc.distributions.list.useQuery();
  if (guides.isError) return <PublicFrame eyebrow="LINUX SETUP" title="Consulta indisponível" description="Não foi possível carregar os guias agora."><ErrorKnowledge returnHref="/setup" returnLabel="Voltar aos guias" /></PublicFrame>;
  return <PublicFrame eyebrow="LINUX SETUP" title="Guias que deixam claro o que fazem." description="Comandos copiáveis, nível de dificuldade, fonte e avisos antes de qualquer alteração no sistema."><section className="mt-6 rounded-2xl border border-primary/15 bg-card/75 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="evidence-label text-primary">BIBLIOTECA CURADA</p><p className="mt-1 text-sm text-muted-foreground">Cada grupo mantém seu escopo de distribuição, runtime e fonte rastreável.</p></div><Badge variant="outline">{guides.data?.length ?? "…"} guias visíveis</Badge></div><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_auto_auto]"><SearchField value={query} onChange={setQuery} placeholder="Pesquisar Steam, Proton, Vulkan, drivers…" /><select aria-label="Filtrar guias por distribuição" className="h-10 rounded-md border bg-background px-3 text-sm" value={distributionId ?? ""} onChange={(event) => setDistributionId(event.target.value ? Number(event.target.value) : undefined)}><option value="">Todas as distros</option>{(distributions.data ?? []).map((distribution) => <option key={distribution.id} value={distribution.id}>{distribution.name}</option>)}</select><select aria-label="Filtrar guias por dificuldade" className="h-10 rounded-md border bg-background px-3 text-sm" value={difficulty ?? ""} onChange={(event) => setDifficulty((event.target.value || undefined) as typeof difficulty)}><option value="">Qualquer dificuldade</option><option value="beginner">Iniciante</option><option value="intermediate">Intermediário</option><option value="advanced">Avançado</option></select></div></section>{guides.isLoading ? <div className="mt-7 grid gap-4 md:grid-cols-2">{Array.from({ length: 4 }).map((_, i) => <Skeleton key={i} className="h-48 rounded-2xl" />)}</div> : <GroupedGuides items={guides.data ?? []} />}{guides.data?.length === 0 ? <EmptyKnowledge icon={Terminal} title="Nenhum guia encontrado" text="Ajuste o termo de busca ou verifique novamente mais tarde." /> : null}</PublicFrame>;
}

function GroupedGuides({ items }: { items: { guide: any; distributionName: string | null }[] }) {
  const groups = items.reduce<Record<string, { guide: any; distributionName: string | null }[]>>((result, item) => { const key = item.distributionName || "Ferramentas e conceitos cross-distro"; (result[key] ||= []).push(item); return result; }, {});
  return <div className="mt-7 space-y-8">{Object.entries(groups).map(([name, group]) => <section key={name}><div className="mb-4 flex flex-wrap items-end justify-between gap-2 border-b border-primary/15 pb-3"><div><p className="evidence-label text-primary">TRILHA TÉCNICA</p><h2 className="mt-1 text-xl font-semibold tracking-tight">{name}</h2></div><span className="text-xs text-muted-foreground">{group.length} guia(s) com fonte</span></div><div className="grid gap-4 md:grid-cols-2">{group.map(({ guide, distributionName }) => <Link key={guide.id} href={`/setup/${guide.slug}`} className="group rounded-2xl border bg-card/90 p-5 transition-all hover:border-primary/40 hover:shadow-md"><div className="flex flex-wrap items-center gap-2"><Badge variant="secondary" className="capitalize">{guide.difficulty}</Badge>{distributionName ? <Badge variant="outline">{distributionName}</Badge> : null}<ProvenanceBadge provenance={guide.provenance} /></div><h3 className="mt-5 font-semibold">{guide.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{guide.description}</p><p className="mt-5 flex items-center text-sm font-medium text-primary">Abrir guia <ChevronRight className="ml-1 h-4 w-4 transition-transform group-hover:translate-x-1" /></p></Link>)}</div></section>)}</div>;
}

export function SetupDetailPage() {
  const [, params] = useRoute("/setup/:slug"); const guideQuery = trpc.knowledge.guides.bySlug.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) }); const guide = guideQuery.data;
  const { isAuthenticated } = useAuth();
  const utils = trpc.useUtils();
  const savedGuides = trpc.user.savedGuides.list.useQuery(undefined, { enabled: isAuthenticated });
  const toggleSavedGuide = trpc.user.savedGuides.toggle.useMutation({ onSuccess: () => { utils.user.savedGuides.list.invalidate(); utils.user.dashboard.invalidate(); } });
  if (guideQuery.isLoading) return <PublicFrame eyebrow="LINUX SETUP" title="Carregando guia" description=""><Skeleton className="mt-8 h-96 rounded-2xl" /></PublicFrame>;
  if (guideQuery.isError) return <PublicFrame eyebrow="LINUX SETUP" title="Consulta indisponível" description="Não foi possível carregar este guia."><ErrorKnowledge returnHref="/setup" returnLabel="Voltar aos guias" /></PublicFrame>;
  if (!guide) return <PublicFrame eyebrow="LINUX SETUP" title="Guia não encontrado" description="Este guia pode não estar publicado."><Link href="/setup"><Button className="mt-6">Voltar aos guias</Button></Link></PublicFrame>;
  const isSaved = savedGuides.data?.some((item) => item.guide.id === guide.id) ?? false;
  const saveGuide = () => isAuthenticated ? toggleSavedGuide.mutate({ guideId: guide.id }) : startLogin();
  return <PublicFrame eyebrow="LINUX SETUP" title={guide.title} description={guide.description || "Guia técnico da plataforma."}><div className="mt-7 flex flex-wrap items-center gap-2"><Badge variant="secondary" className="capitalize">{guide.difficulty}</Badge><ProvenanceBadge provenance={guide.provenance} />{guide.guideVersion ? <Badge variant="outline">Versão: {guide.guideVersion}</Badge> : null}<Button variant={isSaved ? "secondary" : "outline"} size="sm" disabled={toggleSavedGuide.isPending} onClick={saveGuide}><BookOpenCheck className="mr-2 h-4 w-4" />{isSaved ? "Remover dos salvos" : "Salvar no painel"}</Button></div><EditorialRelations game={guide.relatedGame} distribution={guide.relatedDistribution} /><GuideStepsPanel guide={guide} />{guide.sourceUrl ? <a className="mt-7 inline-flex items-center text-sm font-medium text-primary hover:underline" href={guide.sourceUrl} target="_blank" rel="noreferrer">Abrir fonte registrada <ExternalLink className="ml-2 h-4 w-4" /></a> : null}</PublicFrame>;
}

export function LinuxFixPage() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState<string | undefined>();
  const input = useMemo(() => ({ page: 1, pageSize: 40, q: query.trim() || undefined, category: category as never }), [category, query]); const fixes = trpc.knowledge.linuxFix.list.useQuery(input);
  if (fixes.isError) return <PublicFrame eyebrow="LINUXFIX" title="Consulta indisponível" description="Não foi possível carregar as soluções agora."><ErrorKnowledge returnHref="/linuxfix" returnLabel="Voltar ao LinuxFix" /></PublicFrame>;
  return <PublicFrame eyebrow="LINUXFIX" title="Diagnóstico antes de tentativa e erro." description="Cada correção mostra sintomas, causa provável, confiança e origem. Use os comandos somente após ler o alerta."><div className="flex flex-col gap-3 sm:flex-row"><SearchField value={query} onChange={setQuery} placeholder="Pesquisar Steam, Proton, stuttering, tela preta…" /><select className="h-10 rounded-md border bg-background px-3 text-sm" value={category ?? ""} onChange={(event) => setCategory(event.target.value || undefined)}><option value="">Todas as categorias</option>{["steam", "proton", "wine", "vulkan", "amd", "nvidia", "anti_cheat", "audio", "stuttering", "crashes", "black_screen", "launch_errors"].map((item) => <option key={item} value={item}>{item.replace("_", " ")}</option>)}</select></div><div className="mt-7 grid gap-4 md:grid-cols-2">{fixes.isLoading ? Array.from({ length: 4 }).map((_, i) => <Skeleton className="h-52 rounded-2xl" key={i} />) : fixes.data?.map((fix) => <Link key={fix.id} href={`/linuxfix/${fix.slug}`} className="group rounded-2xl border bg-card p-5 transition-all hover:border-primary/40 hover:shadow-md"><div className="flex items-center justify-between gap-2"><Badge variant="secondary" className="capitalize">{fix.category.replace("_", " ")}</Badge><ProvenanceBadge provenance={fix.provenance} /></div><h2 className="mt-5 font-semibold">{fix.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{fix.symptoms}</p><div className="mt-5 flex items-center justify-between"><span className="text-xs text-muted-foreground">Confiança: <strong className="capitalize text-foreground">{fix.confidence}</strong></span><ChevronRight className="h-4 w-4 text-primary transition-transform group-hover:translate-x-1" /></div></Link>)}</div>{fixes.data?.length === 0 ? <EmptyKnowledge icon={Wrench} title="Nenhuma solução encontrada" text="O problema pode ainda não ter uma solução publicada com fonte suficiente." /> : null}</PublicFrame>;
}

export function LinuxFixDetailPage() {
  const [, params] = useRoute("/linuxfix/:slug"); const fixQuery = trpc.knowledge.linuxFix.bySlug.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) }); const fix = fixQuery.data;
  const { isAuthenticated } = useAuth();
  const utils = trpc.useUtils();
  const recordHistory = trpc.user.linuxFixHistory.record.useMutation({ onSuccess: () => { utils.user.linuxFixHistory.list.invalidate(); } });
  if (fixQuery.isLoading) return <PublicFrame eyebrow="LINUXFIX" title="Carregando solução" description=""><Skeleton className="mt-8 h-96 rounded-2xl" /></PublicFrame>;
  if (fixQuery.isError) return <PublicFrame eyebrow="LINUXFIX" title="Consulta indisponível" description="Não foi possível carregar esta solução."><ErrorKnowledge returnHref="/linuxfix" returnLabel="Voltar ao LinuxFix" /></PublicFrame>;
  if (!fix) return <PublicFrame eyebrow="LINUXFIX" title="Solução não encontrada" description="Este conteúdo pode não estar publicado."><Link href="/linuxfix"><Button className="mt-6">Voltar ao LinuxFix</Button></Link></PublicFrame>;
  const addToHistory = () => isAuthenticated ? recordHistory.mutate({ fixId: fix.id }) : startLogin();
  return <PublicFrame eyebrow="LINUXFIX" title={fix.title} description="Solução técnica com origem e confiança indicadas."><div className="mt-7 flex flex-wrap items-center gap-2"><Badge variant="secondary" className="capitalize">{fix.category.replace("_", " ")}</Badge><ProvenanceBadge provenance={fix.provenance} /><Badge variant="outline" className="capitalize">Confiança: {fix.confidence}</Badge><Button variant="outline" size="sm" disabled={recordHistory.isPending} onClick={addToHistory}><Wrench className="mr-2 h-4 w-4" />{recordHistory.isSuccess ? "Adicionado ao histórico" : "Adicionar ao histórico"}</Button></div><EditorialRelations game={fix.relatedGame} distribution={fix.relatedDistribution} /><LinuxFixFeedback fix={fix} /><FixReportPanel fixId={fix.id} /><div className="mt-6 grid gap-5 lg:grid-cols-2"><Card><CardHeader><CardTitle>Sintomas</CardTitle></CardHeader><CardContent className="text-sm leading-6 text-muted-foreground">{fix.symptoms}</CardContent></Card><Card><CardHeader><CardTitle>Possíveis causas</CardTitle></CardHeader><CardContent className="text-sm leading-6 text-muted-foreground">{fix.possibleCauses}</CardContent></Card></div><section className="mt-6 space-y-4">{fix.solutions.map((solution) => <Card key={solution.id}><CardContent className="p-5"><div className="flex gap-4"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-primary/10 text-sm font-semibold text-primary">{solution.stepOrder}</span><div className="min-w-0 flex-1"><h2 className="font-semibold">{solution.title}</h2>{solution.explanation ? <p className="mt-2 text-sm leading-6 text-muted-foreground">{solution.explanation}</p> : null}{solution.command ? <CommandBlock command={solution.command} /> : null}{solution.warning ? <div className="mt-4 flex gap-2 rounded-lg border border-amber-500/25 bg-amber-500/5 p-3 text-sm text-amber-800 dark:text-amber-200"><FileWarning className="mt-0.5 h-4 w-4 shrink-0" />{solution.warning}</div> : null}</div></div></CardContent></Card>)}</section>{fix.sourceUrl ? <a className="mt-7 inline-flex items-center text-sm font-medium text-primary hover:underline" href={fix.sourceUrl} target="_blank" rel="noreferrer">Abrir fonte registrada <ExternalLink className="ml-2 h-4 w-4" /></a> : null}</PublicFrame>;
}

function EditorialRelations({ game, distribution }: { game: { slug: string; title: string } | null; distribution: { slug: string; name: string } | null }) { if (!game && !distribution) return null; return <div className="mt-5 flex flex-wrap items-center gap-2 rounded-xl border border-primary/15 bg-primary/[0.025] p-3 text-sm"><span className="font-medium text-muted-foreground">Relações editoriais:</span>{game ? <Link href={`/games/${game.slug}`} className="rounded-md border bg-background px-2 py-1 text-primary hover:bg-accent">Jogo: {game.title}</Link> : null}{distribution ? <Link href={`/wiki/${distribution.slug}`} className="rounded-md border bg-background px-2 py-1 text-primary hover:bg-accent">Distro: {distribution.name}</Link> : null}</div>; }

function GuideStepsPanel({ guide }: { guide: any }) { const { isAuthenticated } = useAuth(); const utils = trpc.useUtils(); const progress = trpc.engagement.guides.progress.useQuery({ guideId: guide.id }, { enabled: isAuthenticated }); const toggle = trpc.engagement.guides.toggleStep.useMutation({ onSuccess: () => utils.engagement.guides.progress.invalidate({ guideId: guide.id }) }); const completed = new Set((progress.data ?? []).map((item) => item.guideStepId)); const percentage = guide.steps.length ? Math.round((completed.size / guide.steps.length) * 100) : 0; return <section className="mt-6"><div className="mb-4 rounded-xl border bg-card/70 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-medium">Progresso do guia</p><p className="mt-1 text-xs text-muted-foreground">{isAuthenticated ? `${completed.size} de ${guide.steps.length} etapa(s) concluída(s) neste perfil.` : "Entre para registrar o progresso entre sessões."}</p></div><span className="text-sm font-semibold text-primary">{percentage}%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full bg-primary transition-[width] duration-300" style={{ width: `${percentage}%` }} /></div></div><div className="space-y-3">{guide.steps.map((step: any) => { const done = completed.has(step.id); return <details key={step.id} open={!done} className="group rounded-xl border bg-card"><summary className="flex cursor-pointer list-none items-center gap-3 p-5"><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full text-sm font-semibold ${done ? "bg-primary text-primary-foreground" : "bg-primary/10 text-primary"}`}>{done ? <Check className="h-4 w-4" /> : step.stepOrder}</span><span className="flex-1 font-semibold">{step.title}</span><ChevronRight className="h-4 w-4 text-muted-foreground transition-transform group-open:rotate-90" /></summary><div className="border-t px-5 pb-5 pt-4"><div className="pl-10">{step.explanation ? <p className="text-sm leading-6 text-muted-foreground">{step.explanation}</p> : null}{step.command ? <CommandBlock command={step.command} /> : null}{step.warning ? <div className="mt-4 flex gap-2 rounded-lg border border-amber-500/25 bg-amber-500/5 p-3 text-sm text-amber-800 dark:text-amber-200"><CircleAlert className="mt-0.5 h-4 w-4 shrink-0" />{step.warning}</div> : null}<Button className="mt-4" size="sm" variant={done ? "secondary" : "outline"} disabled={toggle.isPending} onClick={() => isAuthenticated ? toggle.mutate({ guideStepId: step.id }) : startLogin()}>{done ? "Marcar como pendente" : "Marcar como concluída"}</Button></div></div></details>; })}</div></section>; }

function LinuxFixFeedback({ fix }: { fix: any }) { const { isAuthenticated } = useAuth(); const utils = trpc.useUtils(); const [body, setBody] = useState(""); const summary = trpc.engagement.linuxFix.summary.useQuery({ fixId: fix.id }); const comments = trpc.engagement.linuxFix.comments.useQuery({ fixId: fix.id }); const myState = trpc.engagement.linuxFix.myState.useQuery({ fixId: fix.id }, { enabled: isAuthenticated }); const invalidate = () => { utils.engagement.linuxFix.summary.invalidate({ fixId: fix.id }); utils.engagement.linuxFix.comments.invalidate({ fixId: fix.id }); utils.engagement.linuxFix.myState.invalidate({ fixId: fix.id }); }; const vote = trpc.engagement.linuxFix.vote.useMutation({ onSuccess: invalidate }); const confirm = trpc.engagement.linuxFix.confirm.useMutation({ onSuccess: invalidate }); const comment = trpc.engagement.linuxFix.comment.useMutation({ onSuccess: () => { setBody(""); invalidate(); } }); const requireLogin = (action: () => void) => isAuthenticated ? action() : startLogin(); return <section className="mt-6 rounded-2xl border border-primary/15 bg-card/75 p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="evidence-label text-primary">FEEDBACK DA COMUNIDADE</p><p className="mt-1 text-sm text-muted-foreground">Votos e confirmações são dados de uso, não substituem a origem técnica da solução.</p></div><div className="flex flex-wrap gap-2"><Button size="sm" variant={myState.data?.vote === 1 ? "default" : "outline"} onClick={() => requireLogin(() => vote.mutate({ fixId: fix.id, value: 1 }))}><ThumbsUp className="mr-1.5 h-4 w-4" />Útil {summary.data?.score ? `(${summary.data.score})` : ""}</Button><Button size="sm" variant={myState.data?.vote === -1 ? "destructive" : "outline"} onClick={() => requireLogin(() => vote.mutate({ fixId: fix.id, value: -1 }))}><ThumbsDown className="mr-1.5 h-4 w-4" />Não resolveu</Button><Button size="sm" variant={myState.data?.confirmed ? "secondary" : "outline"} onClick={() => requireLogin(() => confirm.mutate({ fixId: fix.id }))}><CheckCircle2 className="mr-1.5 h-4 w-4" />{myState.data?.confirmed ? "Confirmação registrada" : `Funcionou (${summary.data?.confirmationCount ?? 0})`}</Button></div></div><div className="mt-5 border-t pt-5"><p className="flex items-center gap-2 font-medium"><MessageCircle className="h-4 w-4 text-primary" />Comentários ({summary.data?.commentCount ?? 0})</p><div className="mt-3 space-y-3">{comments.data?.length ? comments.data.map((item) => <div key={item.id} className="rounded-xl border bg-background/60 p-3"><p className="text-sm">{item.body}</p><p className="mt-2 text-xs text-muted-foreground">{item.userName || "Conta sem nome público"} · {new Date(item.createdAt).toLocaleString("pt-BR")}</p></div>) : <p className="rounded-xl border border-dashed p-4 text-sm text-muted-foreground">Ainda não há comentários publicados para esta solução.</p>}</div><div className="mt-4 space-y-2"><Textarea value={body} onChange={(event) => setBody(event.target.value)} placeholder="Compartilhe o resultado no seu ambiente (mínimo de 8 caracteres)." /><Button size="sm" disabled={body.trim().length < 8 || comment.isPending} onClick={() => requireLogin(() => comment.mutate({ fixId: fix.id, body }))}>Publicar comentário</Button>{comment.isError ? <p className="text-xs text-destructive">Não foi possível publicar o comentário agora.</p> : null}</div></div></section>; }

function PublicFrame({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: React.ReactNode }) { const root = eyebrow === "LINUX WIKI" ? { label: "Wiki", href: "/wiki" } : eyebrow === "LINUX SETUP" ? { label: "Setup", href: "/setup" } : { label: "LinuxFix", href: "/linuxfix" }; return <div className="min-h-screen bg-background"><SiteHeader /><main className="container technical-grid py-8 md:py-12"><PageBreadcrumbs items={[...(title === root.label ? [] : [root]), { label: title }]} /><header className="diagnostic-panel rounded-2xl border border-primary/15 p-5 md:p-7"><div className="relative z-10"><span className="evidence-label inline-flex rounded border border-primary/25 bg-primary/5 px-2 py-1 text-primary">{eyebrow}</span><h1 className="mt-4 max-w-4xl text-4xl font-semibold tracking-tight md:text-5xl">{title}</h1>{description ? <p className="mt-3 max-w-3xl leading-7 text-muted-foreground">{description}</p> : null}</div></header>{children}</main></div>; }
function SearchField({ value, onChange, placeholder }: { value: string; onChange: (value: string) => void; placeholder: string }) { return <div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><Input value={value} onChange={(event) => onChange(event.target.value)} className="pl-9" placeholder={placeholder} /></div>; }
function EmptyKnowledge({ icon: Icon, title, text }: { icon: typeof Wrench; title: string; text: string }) { return <div className="mt-8 rounded-2xl border border-dashed p-10 text-center"><Icon className="mx-auto h-7 w-7 text-muted-foreground" /><h2 className="mt-4 font-semibold">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-muted-foreground">{text}</p></div>; }
function ErrorKnowledge({ returnHref, returnLabel }: { returnHref: string; returnLabel: string }) { return <div className="mt-8 rounded-2xl border border-amber-500/25 bg-card p-8 text-center"><CircleAlert className="mx-auto h-7 w-7 text-amber-500" /><p className="evidence-label mt-4 text-primary">FALHA DE CONSULTA</p><p className="mt-2 text-sm leading-6 text-muted-foreground">A plataforma não recebeu os dados necessários para esta tela. Nenhuma ausência de conteúdo deve ser inferida deste estado.</p><Link href={returnHref}><Button className="mt-5" variant="outline">{returnLabel}</Button></Link></div>; }
function FixReportPanel({ fixId }: { fixId: number }) { const { isAuthenticated } = useAuth(); const utils = trpc.useUtils(); const [open, setOpen] = useState(false); const [description, setDescription] = useState(""); const create = trpc.user.reports.create.useMutation({ onSuccess: () => { utils.user.reports.list.invalidate(); setDescription(""); setOpen(false); } }); const submit = () => isAuthenticated ? create.mutate({ subjectType: "linux_fix", subjectId: fixId, type: "incorrect_information", description }) : startLogin(); return <Card className="mt-6 border-primary/15 bg-card/75"><CardContent className="p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="evidence-label text-primary">REVISÃO COMUNITÁRIA</p><p className="mt-1 text-sm text-muted-foreground">Reporte instrução desatualizada, link quebrado ou solução inadequada.</p></div><Button size="sm" variant="outline" onClick={() => isAuthenticated ? setOpen((value) => !value) : startLogin()}>Reportar solução</Button></div>{open ? <div className="mt-4 space-y-3 border-t pt-4"><Textarea value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Descreva a correção necessária (mínimo de 8 caracteres)." /><div className="flex gap-2"><Button size="sm" disabled={description.trim().length < 8 || create.isPending} onClick={submit}>Enviar report</Button><Button size="sm" variant="ghost" onClick={() => setOpen(false)}>Cancelar</Button></div>{create.isError ? <p className="text-xs text-destructive">Não foi possível enviar o report agora. Tente novamente.</p> : null}</div> : null}</CardContent></Card>; }
function CommandBlock({ command }: { command: string }) { const [copied, setCopied] = useState(false); const copy = async () => { await navigator.clipboard.writeText(command); setCopied(true); window.setTimeout(() => setCopied(false), 1800); }; return <div className="mt-4 overflow-hidden rounded-lg border bg-muted/40"><div className="flex items-center justify-between gap-3 border-b bg-muted/50 px-3 py-2"><span className="flex items-center gap-2 text-xs font-medium text-muted-foreground"><Terminal className="h-3.5 w-3.5" />Comando</span><Button variant="ghost" size="sm" className="h-7 text-xs" onClick={copy}>{copied ? <Clipboard className="mr-1.5 h-3.5 w-3.5" /> : <Copy className="mr-1.5 h-3.5 w-3.5" />}{copied ? "Copiado" : "Copiar"}</Button></div><pre className="overflow-x-auto p-3 text-xs leading-6 text-foreground"><code>{command}</code></pre></div>; }
