import { SiteHeader } from "@/components/platform/SiteHeader";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";

const base = "https://linuxtoys-ckuyvpj5.manus.space";
const routes = [
  ["GET", "/api/v1/games", "Jogos publicados; aceita q e limit (1–100)."],
  ["GET", "/api/v1/distros", "Distribuições publicadas e links de origem."],
  ["GET", "/api/v1/hardware", "Catálogo técnico publicado com fonte quando disponível."],
  ["GET", "/api/v1/linuxfix", "Runbooks publicados com proveniência e confiança."],
  ["GET", "/api/v1/benchmarks", "Somente benchmarks verificados; pode retornar lista vazia."],
  ["GET", "/api/v1/compatibility", "Registros com nível, confiança, origem e data."],
  ["GET", "/api/v1/widgets/games/:slug", "JSON para integrações de um jogo publicado."],
  ["GET", "/api/v1/badges/games/:slug.svg", "Badge SVG de estado e proveniência."],
] as const;

export default function ApiDocs() { return <div className="min-h-screen bg-background"><SiteHeader /><main className="container max-w-[1120px] technical-grid py-7"><header className="border-b border-white/8 pb-6"><p className="font-tech text-[10px] uppercase tracking-[.14em] text-cyan-200">STRAY LINUX / API PÚBLICA</p><h1 className="mt-2 text-3xl font-semibold">Documentação da API v1</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A API é somente leitura. Ela expõe apenas registros publicados e preserva estado, data e origem quando os campos existem. Ausência de evidência não é convertida em estimativa.</p></header><section className="mt-6 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><Card><CardHeader><CardTitle>Recursos</CardTitle><CardDescription>Todos retornam JSON, exceto o badge SVG.</CardDescription></CardHeader><CardContent className="space-y-2">{routes.map(([method, path, description]) => <div key={path} className="rounded-lg border border-white/8 p-3"><div className="flex flex-wrap items-center gap-2"><Badge variant="secondary" className="font-tech text-[10px]">{method}</Badge><code className="text-xs text-cyan-100">{path}</code></div><p className="mt-2 text-xs text-muted-foreground">{description}</p></div>)}</CardContent></Card><div className="space-y-5"><Card><CardHeader><CardTitle>Limites e cache</CardTitle></CardHeader><CardContent className="space-y-3 text-sm leading-6 text-muted-foreground"><p><strong className="text-foreground">60 requisições/minuto</strong> por IP. As respostas incluem <code>X-RateLimit-Limit</code> e <code>X-RateLimit-Remaining</code>.</p><p>A versão é informada por <code>X-API-Version: 1</code>. Respostas JSON públicas usam cache de até 60 segundos; badges, até 300 segundos.</p><p>Em 429, a resposta informa <code>rate_limit_exceeded</code>. Em recurso ausente, o widget devolve 404.</p></CardContent></Card><Card><CardHeader><CardTitle>Widget e badge</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-xs text-muted-foreground">Substitua <code>slug-do-jogo</code> por um slug publicado.</p><pre className="overflow-x-auto rounded-lg bg-black/35 p-3 text-[11px] text-cyan-100">{`${base}/api/v1/widgets/games/slug-do-jogo`}</pre><pre className="overflow-x-auto rounded-lg bg-black/35 p-3 text-[11px] text-cyan-100">{`<img src="${base}/api/v1/badges/games/slug-do-jogo.svg" alt="Stray Linux" />`}</pre></CardContent></Card></div></section><Card className="mt-5"><CardHeader><CardTitle>Proveniência e indisponibilidade</CardTitle></CardHeader><CardContent className="text-sm leading-6 text-muted-foreground">Compatibilidade e benchmark carregam somente dados gravados no catálogo. Widgets retornam <code>evidence_pending</code> quando não há registro de compatibilidade; badges exibem o mesmo estado em vez de sugerir que um jogo é compatível. Campos <code>provenance</code>, <code>confidence</code>, <code>sourceUrl</code> e datas devem ser apresentados por consumidores sempre que presentes.</CardContent></Card></main></div>; }
