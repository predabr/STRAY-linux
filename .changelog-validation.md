# Validação visual do changelog 1.2.0

| Ambiente | Resultado |
|---|---|
| Site, desktop 1280 px | Título, cartões de mudanças e tabela de validação renderizam com hierarquia clara. A linha de Arch usa status `Pendente` e descreve o limite real. |
| Site, mobile 375 px | Os cartões empilham sem corte horizontal; a tabela torna-se uma sequência legível por item; o link de downloads permanece acessível. |
| Aplicativo | A rota `/changelog` é renderizada pelo `ProductWorkspace` quando `window.strayDesktop` existe e o atalho aparece no status lateral. |

O changelog não declara teste `pacman -Qp` como concluído no sandbox. Ele preserva a exigência de validação em uma instalação Arch real.
